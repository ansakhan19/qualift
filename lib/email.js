// Qualift doesn't use an email provider (Resend was removed). Email addresses
// are only tracked to the database and logged to a Google Sheet, see
// lib/sheetsLog.js and app/api/send-magic-link/route.js. This file is kept
// as an empty placeholder so nothing breaks if something still imports it.
